from django import forms

# class EventForm(forms.Form):
#     summary = forms.CharField(label='Summary', max_length=200)
#     description = forms.CharField(label='Description', required=False, widget=forms.Textarea)
#     start_datetime = forms.DateTimeField(label='Start Date and Time')
#     end_datetime = forms.DateTimeField(label='End Date and Time')

    # You can add other fields as needed, such as location, reminders, etc.
